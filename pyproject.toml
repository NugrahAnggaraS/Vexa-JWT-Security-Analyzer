[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "vexa"
version = "0.1.0"
description = "Vexa — JWT security analyzer. Run it with the vexa command."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "cryptography>=42",
]

[project.scripts]
vexa = "jwt_analyzer.main:main"

[project.optional-dependencies]
dev = [
    "pytest>=7.4",
    "pytest-cov>=4.1",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-q"

[tool.coverage.run]
source = ["jwt_analyzer"]

[tool.coverage.report]
fail_under = 80
show_missing = true
